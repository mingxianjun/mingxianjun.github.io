@import '~@/assets/styles/common/mixins.styl';

dashboard {
  margin-top: 1rem;
  padding: 0 !important;

  .page-wrap {
    flexLayout(row, 1);

    .config-box {
      flexLayout(column, none);
      width: 30%;
      background: rgba(#000, 0.3);
    }

    .content-box {
      flexLayout(column, 1);
      margin-left: 1rem;
      background: rgba(#000, 0.3);
    }
  }
}